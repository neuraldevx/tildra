import { SignUp } from '@clerk/nextjs'

export default function Page() {
  // Render the Clerk Sign Up component
  // path="/sign-up" is the default
  // routing="path" is the default
  return <SignUp />
} 